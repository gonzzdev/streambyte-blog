---
// Crear página de blog y añadir links
const ARTICULOS = [
    {
        title: "Crea tu propio linktree",
        description: "Crea tu propia página de links fácilmente con HTML y CSS. Aprende a diseñar y estructurar tu propia página de enlaces, optimizada para dispositivos móviles y fácil de actualizar. Ideal para aquellos que desean tener más control sobre su presencia en línea y mantener todo bajo su propio dominio.",
        link: "Linktree",
        image: "/images/tree.png",
    },
]
---

<div class="flex flex-col gap-y-16 mb-10">
    {
      ARTICULOS.map(({ image, title, description, link }) => (
      <article class="flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0">
    <div class="w-full md:w-1/2">
      <div 
          class="relative flex flex-col items-center 
          col-span-6 row-span-5 gap-8 
          transition duration-500 ease-in-out transform 
          shadow-xl overflow-clip rounded-xl 
          sm:rounded-xl md:group-hover:-translate-x-1 md:group-hover:shadow-2xl 
          lg:border-[2px] lg:border-[#ddede2]/40 lg:hover:border-[#ddede2]/80">
  
        <a href={link}>
          <img alt="Project screenshot" class="object-cover object-top w-full sm:w-fit h-56 transition duration-500 sm:h-full md:scale-110 md:group-hover:scale-105" src={image} />
        </a>
      </div>
    </div>
  
    <div class="w-full md:w-1/2 md:max-w-lg">
      <h3 class="text-2xl font-bold text-[#ddede2] hover:text-[#36f2b3] transition duration-500">
        {title}
      </h3>
      <div class="flex flex-wrap">
  
        <div class="mt-2 text-[#ddede2] font-medium">{description}</div>
  
      </div>
    </div>
  </article>
      ))
    }
  </div>
